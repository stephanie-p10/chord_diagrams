from typing import Iterable


class AddAssumptionsConstructor:
    """Placeholder used by some strategy code paths."""

    def __init__(self, assumptions: Iterable = ()) -> None:
        self.assumptions = tuple(assumptions)


class AddAssumptionsStrategy:
    """Placeholder used by some strategy code paths."""

    def __init__(self, assumptions: Iterable = ()) -> None:
        self.assumptions = tuple(assumptions)

