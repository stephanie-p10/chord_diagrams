"""Legacy figure-generation script (disabled).

This repository previously tracked a large, side-effectful script here that generated
LaTeX/PDF outputs. It has been removed from import-time execution to keep the library
safe to import in any context (including tests and tooling).

If you need the original script, resurrect it under `examples/` and ensure it is guarded
by `if __name__ == "__main__":`.
"""


if __name__ == "__main__":
    pass

